第二步总结
    使用FSM有限状态机来实现HTML的分析
    在HTML标准中已经规定了HTML的状态 80个
第三步总结 // 词法分析
    主要的标签有：开始标签、结束标签、自封闭标签
    可以在各个状态添加逻辑区，处理业务

第四步
    使用栈构建DOM树

第五步
    属性值分为单引号、双引号、无引号三种写法，因此需要较多状态处理
    处理属性的方式跟标签类似
    属性结束时，我们把属性加到标签Token上

第六步
    从标签构建DOM树的基本技巧是使用栈
    遇到开始标签时创建元素并入栈，遇到结束标签时出栈
    自封闭节点可视为入栈后立刻出栈
    任何元素的父元素是他入栈前的栈顶

第七步
    文本节点与自封闭标签处理类似
    多个文本节点需要合并

CSS
第一步
    遇到style标签把css储存起来
    调用CSS Parser 来分析CSS规则
    需要比对css库分析CSS格式的规则的格式

第二步：添加调用
    在创建元素时立即计算css
    当分析一个元素时，所有的css规则已经收集完毕

第三步
    在computeCSS函数中，我们必须知道元素的所有父元素才能判断元素与规则是否匹配
    我们从上一步骤的stack可以获取本元素的所有父元素
    我们获取的元素时当前元素、所以我们获得和计算父元素匹配的顺序是从内向外